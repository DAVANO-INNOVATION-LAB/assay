# Build output
bin/
dist/
# Root-level binaries from an ad-hoc `go build -o <name> ./cmd/...`. Two of
# these were committed once at 78MB; the Makefile targets all write to bin/.
/assay
/assay-manager
/assay-runner
/runner
/manager

# Test and coverage artifacts
cover.out
*.test

# Local tool installs
hack/tools/

# Editor and OS noise
.DS_Store
.idea/
.vscode/
*.swp

# Never commit credentials or kubeconfigs
*.kubeconfig
kubeconfig
.env
.env.*
!.env.example
*.pem
*.key
!**/testdata/*.key

# Internal engineering handoff; not part of the release.
HANDOFF.md
