---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: artifactexceptions.security.davano.io
spec:
  group: security.davano.io
  names:
    kind: ArtifactException
    listKind: ArtifactExceptionList
    plural: artifactexceptions
    singular: artifactexception
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelName
      name: Model
      type: string
    - jsonPath: .spec.modelVersion
      name: Version
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.expiresAt
      name: Expires
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ArtifactException waives specific policy failures for a model
          version.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ArtifactExceptionSpec waives specific policy failures for
              a model version.
            properties:
              approvedBy:
                type: string
              expiresAt:
                description: ExpiresAt after which the exception no longer applies.
                format: date-time
                type: string
              findingIDs:
                description: |-
                  FindingIDs lists waived finding IDs (e.g. CVE-2025-1234). Empty with
                  Rules set waives whole rule categories instead.
                items:
                  type: string
                type: array
              modelName:
                type: string
              modelVersion:
                type: string
              reason:
                type: string
              rules:
                description: Rules lists waived policy rule names (e.g. maxCriticalCVEs,
                  requireSBOM).
                items:
                  type: string
                type: array
            required:
            - modelName
            - modelVersion
            - reason
            type: object
          status:
            description: ArtifactExceptionStatus tracks exception lifecycle.
            properties:
              phase:
                enum:
                - Active
                - Expired
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
