---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: promotionrequests.security.zeus.io
spec:
  group: security.zeus.io
  names:
    kind: PromotionRequest
    listKind: PromotionRequestList
    plural: promotionrequests
    singular: promotionrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelName
      name: Model
      type: string
    - jsonPath: .spec.modelVersion
      name: Version
      type: string
    - jsonPath: .spec.targetEnvironment
      name: Target
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PromotionRequest asks to promote a model version to an environment.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PromotionRequestSpec asks to promote a model version to an
              environment.
            properties:
              justification:
                type: string
              modelName:
                type: string
              modelVersion:
                type: string
              requestor:
                type: string
              targetEnvironment:
                enum:
                - dev
                - stage
                - prod
                type: string
            required:
            - modelName
            - modelVersion
            - targetEnvironment
            type: object
          status:
            description: PromotionRequestStatus tracks the approval workflow.
            properties:
              message:
                type: string
              phase:
                enum:
                - Pending
                - Approved
                - Rejected
                type: string
              reviewTime:
                format: date-time
                type: string
              reviewedBy:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
